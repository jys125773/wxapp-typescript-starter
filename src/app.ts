// import request from './utils/request/index';

// request.post('/api1', {});
// request.post('/api2', {});
// request.post('/api3', {});
// request.post('/api4', {});

// console.log('request instance', request);
App({
  onLaunch() {},
  globalData: {},
});
