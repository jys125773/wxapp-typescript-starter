<view class="container">
  <ui-cell-group border wx:for="{{ cateList }}" wx:key="index" wx:for-item="cateItem" title="{{ cateItem.name }}">
    <ui-cell wx:for="{{cateItem.items}}" wx:key="index" size="large" title="{{ item.name }}" is-link url="{{ item.path }}" />
  </ui-cell-group>
</view>