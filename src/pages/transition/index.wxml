<view class="container">
  <button bind:tap="toggle">{{ in ? 'leave' : 'enter' }}</button>
  <transition name="slide-fade" duration="" show="{{ in }}">
    <view class="box">
    </view>
  </transition>
</view>